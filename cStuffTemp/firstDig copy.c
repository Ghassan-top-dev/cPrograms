#include <stdio.h>

int firsrDigit (int);

int main(){





    printf("Enter a number: ");
    scanf("%d",*******)

    return 0;
}


int firstDigit(int num){


    int first;
    while ( num!= 0)

}
// Adding a timestamp to track commits
// Adding a timestamp to track commits
// Adding a timestamp to track commits
// Adding a timestamp to track commits
// Adding a timestamp to track commits
// Adding a timestamp to track commits
// Adding a timestamp to track commits